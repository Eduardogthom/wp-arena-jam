name: wp_arena_flutter
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_rating_bar: 4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_native_splash: ^0.2.8

flutter:
  uses-material-design: true

  assets:
    - images/
    - images/games/
    - images/users/
    - images/avatars/

  fonts:
    - family: Montserrat
      fonts:
        - asset: fonts/Montserrat-Regular.ttf

# flutter_icons:
#   image_path_android: "images/1024.png"
#   image_path_ios: "images/1024.png"
#   android: true
#   ios: true

flutter_native_splash:
  color: "#18181C"
  image: images/splash.png
  android: true
  ios: true
  android_gravity: center
  ios_content_mode: center
